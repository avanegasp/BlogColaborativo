<div class="container">
  <div class="row">
    <div class="col-sm-4 col-sm-offset-4">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "form-signin"}) do |f| %>
        <h2 class="form-signin-heading">Por favor Inicia Sesión</h2>
        <%= f.label :email, 'Email Address', class: 'sr-only' %>
        <%= f.email_field :email, autofocus: true, class: 'form-control', placeholder: 'Correo Electrónico' %>
        <%= f.label :password, 'Password', class: 'sr-only' %>
        <%= f.password_field :password, autocomplete: "off", class: 'form-control', placeholder: 'Contraseña' %>
        <% if devise_mapping.rememberable? -%>
        <br>
          <label>
            <%= f.check_box :remember_me, label: false %>
            Recuerdame
          </label>
          <br>
        <% end -%>
        <%= f.button "Iniciar Sesión", class: 'btn-sub btn btn-lg btn-primary btn-block' %>
      <% end %>
      <br>
      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
